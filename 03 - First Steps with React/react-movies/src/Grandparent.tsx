import Parent from './Parent';
//See also the app.tsx and Parent to understand the implementation of ValueContext.Provider

export default function GrandParent(){
    return (
        <Parent />
    )
}